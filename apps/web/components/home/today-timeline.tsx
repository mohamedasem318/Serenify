"use client";

/**
 * Feature 009 — the expanded today card's per-session timeline (state-coloured chips).
 * Built in User Story 2 (tasks T014/T017). Placeholder skeleton for now; US1 ships
 * only the collapsed surface.
 */
export function TodayTimeline() {
  return null;
}
