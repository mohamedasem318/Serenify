# Serenify Anchor API — copy to .env and fill in. Never commit real values.

# Legacy HS256 secret — the symmetric verification fallback for projects that
# still sign access tokens HS256. Supabase dashboard: Project Settings -> API ->
# JWT Settings -> JWT Secret. (Local CLI default below.)
SUPABASE_JWT_SECRET=

# Web origin allowed by CORS — must equal the origin you LOAD the app at (the page
# origin), NOT the API host. Dev default below: open the app at http://localhost:3000.
# For LAN/device testing set it to the LAN page origin, e.g. http://192.168.x.x:3000
# (which also requires HTTPS for the camera — see apps/web/README).
ALLOWED_ORIGIN=http://localhost:3000

# Supabase project base URL — used to fetch the JWKS public keys that verify the
# asymmetric ES256 access tokens current Supabase issues by default. Dev:
# http://127.0.0.1:54321. Leave blank only for an HS256-only project.
SUPABASE_URL=
